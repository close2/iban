name: iban
description: A validation library for IBANs included in the official registry. Also supports some unofficial ones.
version: 1.1.0
homepage: https://github.com/close2/iban

environment:
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  mod97: '^1.0.0'

dev_dependencies:
  test: '^1.16.0'
  pedantic: ^1.8.0
